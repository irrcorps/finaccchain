# Composer dependencies (run `composer install` to restore, see README)
/vendor/

# Runtime-uploaded transaction evidence (regenerated by database/seed_demo_transactions.php for demo data)
/uploads/evidence/*
!/uploads/evidence/.gitkeep

# OS/editor cruft
.DS_Store
Thumbs.db
*.log

# Local environment overrides (never commit real credentials)
.env
.env.*
!.env.example
